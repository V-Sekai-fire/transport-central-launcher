# SCM syntax highlighting & preventing 3-way merges
pixi.lock merge=binary linguist-language=YAML linguist-generated=true -diff

# mix format reads LF; core.autocrlf=true on a Windows desk otherwise checks
# these out as CRLF and --check-formatted fails on files CI is happy with.
* text=auto eol=lf
pixi.lock -text
